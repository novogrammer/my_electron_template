{
  "name": "my_electron_template",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --out bin --overwrite",
    "clean": "del ./bin/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "novogrammer@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^1.1.0",
    "electron": "^2.0.2",
    "electron-packager": "^12.1.0"
  },
  "dependencies": {
    "jquery": "^3.3.1"
  }
}
